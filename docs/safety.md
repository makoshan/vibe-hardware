# 硬件安全规则

- ESP32 GPIO 只能使用 **3.3V 逻辑电平**。
- **不要**把 5V 直接接入 GPIO。
- 喇叭必须通过功放模块连接。
- 舵机、电机不能直接由 ESP32 的 3.3V 引脚供电。
- 修改接线前先断开 USB 电源。
- 不确定电压时，先用万用表测量。
- 烧录失败时，先检查 USB 线是否支持数据传输。
- 第一次使用模块时，先运行官方示例，再改项目代码。

## 建议常备工具（不必一次买齐）

万用表、面包板、卡尺。示波器 / 显微镜按问题再借。
